{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "/favicon.ico", "/robots.txt", "/manifest.webmanifest", "/wasm/*"]
  },
  "globalHeaders": {
    "Permissions-Policy": "camera=(self), microphone=(self), autoplay=(self)"
  }
}
